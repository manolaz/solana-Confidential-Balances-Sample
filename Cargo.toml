[workspace]
resolver = "2"
members = [
    "ingredients/setup_participants",
    "ingredients/transfer_public_mint",
    "utils",
    "recipes"
]

# Workspace-wide dependencies can be specified here if needed
[workspace.dependencies]
