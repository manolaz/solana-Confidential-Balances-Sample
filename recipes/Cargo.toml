[package]
name = "recipes"
version = "0.1.0"
edition = "2021"

[dependencies]
setup_participants = { path = "../ingredients/setup_participants" }
setup_mint = { path = "../ingredients/setup_mint" }
tokio = { version = "1", features = ["full"] } 
setup_token_account = { path = "../ingredients/setup_token_account" }
mint_tokens = { path = "../ingredients/mint_tokens" }
deposit_tokens = { path = "../ingredients/deposit_tokens" }
apply_pending_balance = { path = "../ingredients/apply_pending_balance" }
transfer = { path = "../ingredients/transfer" }
keypair_utils = { path = "../utils" }
solana-sdk = "2.1.2"
withdraw_tokens = { path = "../ingredients/withdraw_tokens" }